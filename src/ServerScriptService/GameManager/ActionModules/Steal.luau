--!strict
--!optimize 2

local types = require(script.Parent.Parent.Types)

local stealModule = {}

function stealModule.Steal(stealInfo:types.StealInfo)
	if stealInfo.requestedCard then
		
	end
end

return stealModule
